"""Marketplace catalog domain — products, pricing, inventory (scaffolded).

Conceptually this is the TradeBay Marketplace domain. The package name
`catalog` is retained for stability; import via `app.modules.marketplace`
when referring to the domain by name.
"""