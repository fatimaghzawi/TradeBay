import { DomainPlaceholder } from "@/components/shared/DomainPlaceholder";

export default function SettingsPage() {
  return (
    <DomainPlaceholder
      domain="settings"
      title="Settings"
      description="Profile, business preferences, and notification defaults."
    />
  );
}
