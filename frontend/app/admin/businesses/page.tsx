import { DomainPlaceholder } from "@/components/shared/DomainPlaceholder";

export default function AdminBusinessesPage() {
  return (
    <DomainPlaceholder
      domain="admin"
      title="Businesses"
      description="Platform administration for registered businesses."
    />
  );
}
