"use client";

import { AdminCommandCenter } from "@/components/admin/AdminCommandCenter";

/**
 * Platform ops + finance command center — not the buyer/supplier desk.
 */
export default function AdminHomePage() {
  return <AdminCommandCenter />;
}
