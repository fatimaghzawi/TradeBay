import { DomainPlaceholder } from "@/components/shared/DomainPlaceholder";

export default function AdminSettlementsPage() {
  return (
    <DomainPlaceholder
      domain="admin"
      title="Settlements"
      description="Settlement batches, payouts, and reconciliation oversight."
    />
  );
}
