import { DomainPlaceholder } from "@/components/shared/DomainPlaceholder";

export default function AdminSuppliersPage() {
  return (
    <DomainPlaceholder
      domain="admin"
      title="Suppliers"
      description="Supplier onboarding, verification, and lifecycle controls."
    />
  );
}
